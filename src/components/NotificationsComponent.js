import React from 'react'

export const NotificationsComponent = () => {
    return (
        <div>
            To-do:
            filter by author, add all replies that aren't by user, display in date order, reverse
        </div>
    )
}
